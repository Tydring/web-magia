---
phase: 03-hero-section
task: 0
total_tasks: 7
status: not_started
last_updated: 2026-02-23T23:00:52.583Z
---

<current_state>
Steps 1 and 2 are fully complete and committed to index.html (662 lines, ~24KB).
Next step is Step 3 — Hero section. No work has started on Step 3 yet.
The project has no GSD phase plan files; all spec lives in CLAUDE.md.
</current_state>

<completed_work>

- Step 1 (Base Structure): DOCTYPE, meta tags, Google Fonts (Montserrat 300–900), full CSS custom properties (colors, radii, timing tokens), CSS reset, utility classes (.container, .section-tag, .section-title, .section-subtitle, .btn variants, .gold-divider, .fade-up), custom scrollbar, gold text selection — Done
- Step 2 (Loader + Nav): Full-screen loader overlay with animated name + gold progress bar (0→100% in 1.2s, fades at 1.8s, sets body.loaded), fixed pill nav with glass blur + gold border + .scrolled state, desktop links with pill active state, mobile hamburger → animated X → pill drawer dropdown, JS for loader/cursor (dot + lerp ring)/nav scroll/hamburger/active-link IntersectionObserver/fade-up IntersectionObserver + MutationObserver — Done

</completed_work>

<remaining_work>

- Step 3: Hero section — 100dvh, CH.jpg bg (brightness 0.35), parallax translateY(scroll*0.4) via CSS var + rAF, canvas particle layer (60 gold particles slow upward), radial gradient overlay, eyebrow tag + H1 name + slogan + 2 CTA buttons + scroll arrow, staggered fade-in after loader (0/150/300/450/600ms)
- Step 4: Shows section — header, asymmetric 12-col gallery (6 photos), 6 service cards (3-col, watermark numbers, hover lift+glow), bio block (60/40, DSC_0281), logo marquee left-scroll, contact form, social links
- Step 5: Clases section — gold divider, header, gallery (5 photos), 2 tier cards, bio block (40/60 FLIPPED, DSC_0306), marquee right-scroll, contact form, social links, footer
- Step 6: Scroll animations — data-delay wiring on all gallery items, service cards, stats
- Step 7: Visual effects — floating particles, cursor hover states, button shimmer, gallery hover overlay
- Step 8: Mobile responsiveness — breakpoints 1024/768/480px, grid collapses, bio stacking, hero text clamp
- Step 9: Final polish — spacing, transitions, typography, cross-section consistency, lazy loading, will-change cleanup

</remaining_work>

<decisions_made>

- Single file delivery: all HTML/CSS/JS in index.html, no external files
- Image paths relative from project root (images in subfolders at root level, not moved)
- No frameworks, no build step, vanilla JS + native CSS only
- Forms UI-only with action="" placeholder (future: EmailJS/Formspree)
- Logo marquee is text-only (Hard Rock Cafe · CAVEP · Farmatodo · Teatrex) — no SVG files
- WhatsApp placeholder 58XXXXXXXXX — real number not yet provided by client
- Custom gold cursor desktop-only via pointer: fine media query
- Confirm with user before proceeding from each step to the next

</decisions_made>

<blockers>

- WhatsApp number: client has not provided real number yet. Using placeholder `58XXXXXXXXX`.

</blockers>

<context>
This is a straightforward sequential build — each step builds on the previous. The CLAUDE.md spec is comprehensive and should be the primary reference. Steps 3–9 are all clearly defined there with exact requirements. The design system (gold palette, 999px pill radius rule, Montserrat font) is already established in the CSS vars from Step 1.

When resuming, read CLAUDE.md first (the full spec), then read index.html to understand what's already in place, then start Step 3 (Hero section).
</context>

<next_action>
Start with: Read CLAUDE.md, then read index.html (662 lines), then implement Step 3 — Hero section. Begin with the HTML structure for #hero, then add the CSS, then the JS (parallax rAF loop + canvas particles + staggered fade-in after loader).
</next_action>
